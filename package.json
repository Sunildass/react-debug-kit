{
  "name": "react-debug-kit-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "A modular React debugging toolkit",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter playground dev",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0"
  }
}
